# Reports

Benchmark report runners write generated artifacts under:

```text
reports/generated/
```

Generated reports are intentionally ignored by Git. Commit the runner, SQL,
documentation, and summarized findings. Keep raw generated report files local
unless a specific experiment needs a curated checked-in artifact.
